version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - pnp-volume:/web/logs
    networks:
      - pnp-network

volumes:
  pnp-volume:

networks:
  pnp-network:
    driver: bridge
